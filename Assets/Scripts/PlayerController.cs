using UnityEngine;
using System.Collections;

public class PlayerController : MonoBehaviour {

	public string input;
	Vector3 direction;

	void Update(){
		if (Input.GetKeyDown(input)) {
			
		}
	}
}
